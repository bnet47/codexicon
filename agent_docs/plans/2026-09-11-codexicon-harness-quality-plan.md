# Implementation Plan: Codexicon harness quality gaps\n\n**Spec:** [SPEC.md](../../SPEC.md)\n**Date:** 2026-09-11\n\n## Global constraints\n\n- Preserve the existing Codexicon contract, adoption safety, security boundaries, and unrelated user files.\n- Build stays local and Git-free until all four tasks are verified.\n- Only one implementation writer works in the shared checkout at a time.\n- Starter generation uses an explicit allowlist and never reads protected credential paths.\n- Live evaluation uses isolated temporary fixtures, no credentials, no external writes, and records unavailable telemetry as `unmeasured`.\n\n## Acceptance mapping\n\n| Criterion | Task | Evidence |\n|---|---|---|\n| A-017 | T-017 | Starter scaffold smoke test and release-safe template validation |\n| A-018 | T-018 | ADR/policy consistency test and full template validation |\n| A-019 | T-019 | Release identity check, simulated drift tests, and release validation |\n| A-020 | T-020 | Isolated live Codex paired trials and aggregate report |\n\n### Task 1: Generate a clean project starter\n\n**Depends on:** none\n**Files:** `scripts/codexicon.py`, `scripts/scaffold.py` or equivalent, `docs/`, `README.md`, `START_HERE.md`, `tests/`\n**Behavior:** Provide a documented scaffold command with an explicit safe source allowlist. Generated output carries reusable Codexicon harness files but excludes the Codexicon repository's active contract, task ledger, internal briefs, plans, checkpoints, receipts, and evaluation records. A generated project starts with `$discover` and can validate its first local baseline.\n**Verification:** `python -m unittest tests.test_scaffold tests.test_template`; `python scripts/validate_template.py --release`\n**Done when:** A fresh temporary scaffold contains no internal development state and its smoke test passes.\n\n### Task 2: Reconcile writer and publication policy\n\n**Depends on:** T-017\n**Files:** `agent_docs/decisions/ADR-004-sequential-build-writers-and-release-boundary.md`, `AGENTS.md`, `.agents/skills/`, `.codex/agents/`, `docs/`, `tests/`\n**Behavior:** Add the append-only superseding decision and make all current guidance agree on sequential Build writers, read-only parallel review, and explicit Ship publication. Add a regression check for contradictory writer language.\n**Verification:** `python -m unittest tests.test_template`; `python scripts/validate_template.py`\n**Done when:** The contradiction is represented historically but cannot remain in current actionable guidance.\n\n### Task 3: Make releases reproducible\n\n**Depends on:** T-017,T-018\n**Files:** `scripts/release.py`, `TEMPLATE_VERSION`, `.codexicon.json`, `README.md`, `docs/maintainers.md`, `.github/workflows/`, `tests/`\n**Behavior:** Add a release identity check that binds the canonical version, manifest, public references, exact Git tag when available, and generated starter artifact. Update the release notes/version and add simulated drift coverage.\n**Verification:** `python scripts/release.py check`; `python scripts/validate_template.py --release`; `python -m unittest tests.test_release`\n**Done when:** Version drift fails locally and the exact version intended for publication passes with a reproducible artifact identity.\n\n### Task 4: Measure real Codex behavior\n\n**Depends on:** T-017,T-019\n**Files:** `scripts/live_agent_eval.py`, `docs/evals/live-agent-benchmark.md`, `docs/evals/live-agent-results-2026-09-11.md`, `tests/`\n**Behavior:** Run isolated direct and Codexicon Codex trials from versioned task fixtures, execute independent acceptance checks, capture elapsed time/interventions/regressions and available token/cost data, and write honest JSONL plus Markdown aggregate results. Do not claim unsupported client or model behavior.\n**Verification:** `python scripts/live_agent_eval.py --smoke`; `python scripts/live_agent_eval.py --report docs/evals/live-agent-results-2026-09-11.md`\n**Done when:** The live report has repeated paired runs, denominators, raw evidence paths, and explicit unmeasured fields.\n\n